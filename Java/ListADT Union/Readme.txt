Project for CS367 at UW Madison. Using a previously created ListADT, design a method that will preform multiple operations: 
-	If list1 or list2 (or both) is null, throw a NullListException.
-	If both list1 and list2 are empty, return a new empty list.
-	 If only list1 is empty, return a new list containing the objects in list2.
-	 If only list2 is empty, return a new list containing the objects in list1.
-	 Otherwise return a new list that contains the objects in list1 and in list2
-	 with all duplicates removed. For example:
